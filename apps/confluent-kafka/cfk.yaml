name: kafka-cluster
profileName: pre-selected-aws-cluster
source:
  type: yaml
  yaml: 
    # The targetNamespace is the namespace where the resources will be deployed
    targetNamespace: confluent
    repo: 
      path: resources/cfk
      repoURL: https://github.com/astropulseinc/astro-platform-apps.git
      targetRevision: main
    exclude: "*.md"
externalAccess:
  accessType: None
